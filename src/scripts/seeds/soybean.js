export const createSoybean = () => {
    const soybeanProperties = {
    type: "Soybean",
    height: 20,
    output: 4
    };

    return soybeanProperties

}

//create the createSoybean() that will be exported
//function should retun an object with the properties of type, height, and output.  